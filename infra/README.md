# Infrastructure

Safe local and deployment templates will live here. Secret values are supplied at runtime and never
committed. The first infrastructure milestone will add PostgreSQL, GoTrue, Mailpit, and container
hardening documentation.
